// Import React

import React from 'react'
// Create Functional component

const CalculatorTitle = (props) => {
  return ( 
  	<div className="calculator-title" > { props.title} </div>
  )
}

export default CalculatorTitle;